import 'src/assets/assets_runner.dart';

/// Also runnable directly (`dart run bin/assets.dart`), independent of the
/// `moshaf_boilerplate` CLI entry point.
Future<void> main(List<String> arguments) => mainAssets(arguments);
